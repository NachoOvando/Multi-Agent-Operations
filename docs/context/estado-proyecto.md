# Estado del proyecto

> Mantenido por el subagente `documentador`. Refleja qué está implementado de verdad vs. qué es un stub — no asumir que un archivo existente tiene lógica real sin revisarlo acá.

## Implementado (código real, funcionando)

- `apps/api/src/services/permissions_service.py`: `Domain`, `Role`, `ROLE_DOMAIN`, `DOMAIN_ACTIONS` (ahora completo para los 3 dominios: Almacén, Compras, Planificación — ver abajo), `CROSS_DOMAIN_READERS`, `get_operator_permissions` (fail-closed), `assert_can_perform`, `assert_can_read`.
- `apps/api/src/db/models.py`: modelo `Operator` (operator_id, role, created_at).
- `apps/api/src/db/session.py`: engine/sesión lazy vía `DATABASE_URL`. Documenta el requisito de prefijo `postgresql+psycopg://` (ver `docs/context/decisiones.md`).
- `apps/api/src/services/cross_domain_reads.py`: `leer_stock_para`, `leer_necesidades_para` — permission-check real (`assert_can_read`), delegan a `stock_queries`/`production_queries` (que siguen siendo stubs — ver abajo). Verificado end-to-end: el `Runner` de Compras recibe exactamente estos dos tools inyectados por el orquestador.
- **`apps/api/src/agents/orchestrator.py`**: dejó de ser esqueleto — `_build_runners()`, `_extra_tools_for(domain)` y `handle_query()` son código real que construye `DatabaseSessionService` + los 3 `LlmAgent`/`Runner` de ADK una sola vez, resuelve el dominio del operador vía `permissions_service`, e invoca `runner.run_async(...)`. Validado instalando `google-adk==2.5.0` de verdad en `apps/api/.venv` (no solo `py_compile`): se construyeron los 3 `Runner` completos contra un `DATABASE_URL` de prueba y se confirmó que Compras recibe `leer_stock_para` + `leer_necesidades_para` inyectados, y que Almacén/Planificación no tienen ninguna referencia cruzada.
- **`apps/api/src/agents/almacen/agent.py`, `agents/compras/agent.py`, `agents/planificacion/agent.py`**: `build_agent(extra_tools=None)` real — construyen un `Agent` (alias `LlmAgent` de ADK) con `model=LiteLlm(model="anthropic/claude-sonnet-5")` y wrappers `_nombre_tool(tool_context: ToolContext, ...)` que extraen `operator_id` de `tool_context.user_id`, abren sesión de DB corta con `db/session.py::get_session()` y delegan a la función real de `tools.py`. Patrón confirmado contra la API real de ADK 2.5.0 (no es solo el diseño documentado — se instaló y se probó).
- `apps/api/src/agents/almacen/tools.py`: `consultar_stock` (permission-check real + delega a `stock_queries.get_stock`, que es stub) y `registrar_movimiento` (permission-check real, cuerpo `NotImplementedError` — falta también el logging de auditoría y el schema de stock).
- **`apps/api/src/agents/compras/tools.py`** (antes vacío, ahora implementado): `consultar_precios_proveedor` y `registrar_orden_compra` — permission-check real (`assert_can_perform`), delegan a `services/purchasing_queries.py` (nuevo, stub — ver abajo).
- **`apps/api/src/agents/planificacion/tools.py`** (antes vacío, ahora implementado): `consultar_necesidades` (reusa `production_queries.get_requirements` — la misma fuente que usa la lectura cruzada de Compras, sin duplicar lógica) y `registrar_necesidad` — permission-check real, delega a `production_queries.py` (stub).
- `apps/api/src/agents/{almacen,compras,planificacion}/prompts.py`: `INSTRUCTION` real con reglas de dominio (antes docstring-only, ahora contenido real usado por `build_agent`).
- `apps/api/requirements.txt`: versiones verificadas por instalación real en esta sesión — `google-adk>=2.5.0`, `fastapi>=0.139.0`, `uvicorn>=0.51.0`, `sqlalchemy>=2.0`, `psycopg[binary]>=3.1`, y `litellm==1.91.4` pineado (ver decisión en `decisiones.md`).
- `apps/api/.env.example` (nuevo): documenta `DATABASE_URL` con prefijo obligatorio `postgresql+psycopg://` y `ANTHROPIC_API_KEY`.
- `apps/api/.venv/`: entorno virtual real con las dependencias instaladas y verificadas (no solo declaradas en requirements.txt).
- **`apps/api/src/services/auth_service.py`** (nuevo, Slice 1): `hash_password`/`verify_password` (bcrypt directo, no passlib), `authenticate_operator(username, password, session)` (fail-closed, mismo hash "señuelo" contra timing attack cuando el username no existe), `issue_access_token(operator)`/`decode_access_token(token)` (PyJWT, HS256, claims `sub`/`role`/`iat`/`exp`, `JWT_SECRET_KEY`/`JWT_EXPIRES_MINUTES` desde env, fail-closed en cualquier claim faltante o rol desconocido).
- **`apps/api/src/security/dependencies.py`** (nuevo, Slice 1): `get_current_operator` — única fuente válida de `operator_id`/`role` en cualquier endpoint protegido (parsea `Authorization: Bearer <token>`, 401 fail-closed en cualquier ausencia/formato/token inválido). Reemplaza cualquier `operator_id` que hubiera podido venir de un path/body param.
- **`apps/api/src/schemas/auth.py`** (nuevo, Slice 1): `LoginRequest`, `LoginResponse`, `OperatorSummary`.
- **`apps/api/src/schemas/agents.py`** (nuevo, Slice 1): `MessageRequest`, `MessageResponse`.
- **`apps/api/src/routers/auth.py`** (nuevo, Slice 1): `POST /api/auth/login` real — delega a `auth_service.authenticate_operator`/`issue_access_token`, sin SQL/lógica de negocio en el router.
- **`apps/api/src/routers/agents.py`** (antes solo `APIRouter()` vacío, ahora real): `POST /api/agents/messages` — `operator_id` sale exclusivamente de `Depends(get_current_operator)` (nunca de un path/body param, cierra el gap de suplantación de identidad), llama `orchestrator.handle_query(operator.operator_id, body.query, session)`, mapea `UnknownOperatorError`→401 y `PermissionDeniedError`→403.
- **`apps/api/src/db/models.py::Operator`**: ganó `username` (unique, index) y `password_hash`. `operator_id` sigue siendo PK.
- **`apps/api/src/db/session.py`**: agregado `get_db()` (generador compatible con `Depends()` de FastAPI, mismo ciclo de vida que `get_session()`) y `get_database_url()`/`_normalize_database_url()` (normaliza defensivamente `postgresql://`→`postgresql+psycopg://` si el proveedor, ej. Railway, entrega el prefijo genérico).
- **`apps/api/src/main.py`**: ahora tiene `lifespan` real que invoca `get_runner_for_domain(domain)` para cada `Domain` al boot (pre-construye los 3 Runners, falla rápido si `DATABASE_URL`/ADK están mal configurados), `CORSMiddleware` (origen único desde `ALLOWED_ORIGIN`, fail-closed sin la env — lista vacía de orígenes si falta, nunca `"*"`; `allow_credentials=False` porque la auth es Bearer no cookie), monta `routers.auth` + `routers.agents`, y `GET /health` (sin auth, no expone datos, usado por el healthcheck de Railway).
- **`apps/api/src/scripts/create_operator.py`** (nuevo, Slice 1): CLI interactivo para que un admin provisione cuentas (sin auto-registro) — password nunca como argumento de CLI, se hashea con `auth_service.hash_password` antes de persistir.
- **Alembic** (nuevo, no existía ninguna herramienta de migración antes): `apps/api/alembic.ini`, `apps/api/alembic/env.py`, `apps/api/alembic/versions/0001_baseline_operators.py`, `0002_operator_auth_fields.py` (agrega `username`/`password_hash`).
- **`apps/api/requirements.txt`**: agregado `PyJWT>=2.8`, `bcrypt>=4.1`, `alembic>=1.13`, `python-multipart`.
- **`apps/api/.env.example`**: agregado `JWT_SECRET_KEY`, `JWT_EXPIRES_MINUTES` (default 480 en código), `ALLOWED_ORIGIN`.
- **Deploy (nuevo, no existía ningún archivo de deploy en el repo antes de esta sesión)**: `railway.json` (raíz — Root Directory de Railway debe ser la raíz del repo porque los imports son absolutos `from apps.api.src...`; build `pip install -r apps/api/requirements.txt`, start `alembic -c apps/api/alembic.ini upgrade head && uvicorn apps.api.src.main:app --host 0.0.0.0 --port $PORT`, healthcheck `/health`), `.python-version` (raíz, `3.12`), `docker-compose.yml` (raíz, Postgres local para dev/migraciones, credenciales explícitamente marcadas dev-only en el propio archivo).
- **`apps/web/auth.ts`** (nuevo, Slice 1): Auth.js v5 (`next-auth@5.0.0-beta.29`), `CredentialsProvider.authorize()` — único punto del frontend que llama directo a `BACKEND_API_URL` (`POST /api/auth/login`); envuelve el `access_token` del backend dentro de la sesión cifrada de NextAuth (`session.backendToken`) sin decodificarlo nunca. `session: {strategy: "jwt"}`.
- **`apps/web/app/api/auth/[...nextauth]/route.ts`** (nuevo, Slice 1): handlers estándar de Auth.js.
- **`apps/web/middleware.ts`** (nuevo, Slice 1): protege todas las rutas salvo `/login` y `/api/auth/*`; redirige a `/login` sin sesión de NextAuth. No reimplementa reglas de permisos de dominio/rol (eso sigue siendo del backend).
- **`apps/web/app/login/page.tsx`** (nuevo, Slice 1): formulario username/password, `signIn("credentials", {redirect:false})`, error genérico.
- **`apps/web/app/api/backend/[...path]/route.ts`** (nuevo, Slice 1): proxy BFF — única ruta por la que el navegador llega al backend de Railway. Lee `auth()` server-side, responde 401 sin llamar al backend si no hay `session.backendToken`, reenvía método/path/query/body (incluido multipart) agregando `Authorization: Bearer <backendToken>`, filtra headers hop-by-hop, nunca agrega `operator_id`.
- **`apps/web/lib/api-client.ts`**, **`apps/web/types/next-auth.d.ts`**, **`apps/web/components/AgentSmokeTest.tsx`**, **`apps/web/app/(chat)/page.tsx`** (nuevos, Slice 1): fetch helpers tipados, module augmentation TS de la sesión, y smoke-test mínimo que valida la cadena auth end-to-end contra `/api/backend/agents/messages` (no es la UI de chat final).
- **`apps/web/package.json`**: agregado `next-auth@5.0.0-beta.29`, `zod@^3.23.8`.
- **`apps/web/.env.example`** (nuevo): `AUTH_SECRET`, `BACKEND_API_URL` (server-only, nunca `NEXT_PUBLIC_*`), `NEXTAUTH_URL` (opcional en Vercel).

## Stub (estructura decidida, sin lógica real — todos documentan explícitamente qué falta)

- `apps/api/src/services/stock_queries.py`: `get_stock`, `upsert_stock_snapshot` — `NotImplementedError`, esperan el schema de stock.
- `apps/api/src/services/production_queries.py`: `get_requirements`, `create_requirement` — `NotImplementedError`, esperan el schema de necesidades de producción.
- `apps/api/src/services/purchasing_queries.py`: `get_supplier_price`, `create_purchase_order` — `NotImplementedError`, esperan el schema de precios de proveedor/órdenes de compra.
- `apps/api/src/services/mrp.py`: `compute_coverage` — `NotImplementedError`, depende de que `stock_queries`/`production_queries` devuelvan datos reales.
- `apps/api/src/agents/base_agent.py`: docstring-only, sin uso real todavía (ningún `agent.py` de dominio lo importa).

## No implementado / no decidido todavía

- Schema de stock, necesidades de producción, precios de proveedor — deliberadamente diferido.
- Pipeline de ingesta de Excel (parseo, mapeo de columnas, upsert) — Slice 2 del plan, no arrancado todavía.
- Logging de auditoría (operator_id + rol + timestamp) en acciones mutantes — pendiente en `registrar_movimiento`, `registrar_orden_compra`, `registrar_necesidad` (los tres tienen el TODO explícito en el código; no forma parte del Slice 1, que solo cubre auth/boot/deploy).
- **`apps/api/tests/`** (pendiente explícito del Slice 1, en follow-up inmediato): no existe todavía ningún `conftest.py`/test de `auth_service`/`permissions_service`. Confirmado por relevamiento directo en esta sesión — el directorio no existe en el repo.
- **Verificación en runtime real del Slice 1** (pendiente explícito, señalado por el propio backend-especialista): Docker no está disponible en el sandbox de desarrollo actual, por lo que `alembic -c apps/api/alembic.ini upgrade head` y el boot real de `uvicorn apps.api.src.main:app` (con el `lifespan` nuevo) no se ejercitaron en vivo en esta sesión — falta correrlos manualmente en un entorno con Docker/Postgres antes de considerar el Slice 1 100% verificado en runtime (el código y su construcción estática sí están confirmados).
- Estrategia definitiva de `session_id` de ADK (hoy `handle_query` usa `operator_id` como `session_id`, marcado como TODO explícito a revisar cuando exista un modelo real de conversaciones).
- Llamada real end-to-end contra la API de Anthropic: se validó la *construcción* de los 3 `Runner`/`LlmAgent` con `google-adk` instalado, pero no se probó una invocación real de `runner.run_async(...)` contra un modelo (requiere `ANTHROPIC_API_KEY` configurada).
- Actualizar el pin de `litellm==1.91.4` cuando haya wheel precompilado disponible para una versión más nueva en el entorno de destino (hoy bloqueado por falta de Cargo/MSVC Build Tools en Windows).
- Deploy real ejecutado (Slice 3 del plan, no arrancado): `railway.json`/`docker-compose.yml`/`.python-version` existen y están relevados en el código, pero no se validaron contra un deploy real de Railway ni un proyecto real de Vercel en esta sesión.

## Última actualización

Slice 1 del plan de auth/hosting/excel (`/root/.claude/plans/necesito-2-subagentes-greedy-lagoon.md`): login real por usuario+password (bcrypt) con JWT propio emitido/verificado por FastAPI, `POST /api/agents/messages` protegido (operator_id exclusivamente vía `Depends(get_current_operator)`, cierra el gap de suplantación de identidad que existía antes), `lifespan` de boot de ADK, CORS restringido, `GET /health`, Alembic, CLI de provisioning de operadores, y archivos de deploy (`railway.json`/`.python-version`/`docker-compose.yml`) — backend en Railway, no Vercel, por los Runners de ADK con `DatabaseSessionService` persistente (incompatible con serverless). En el frontend: login real con Auth.js v5 y proxy BFF (`app/api/backend/[...path]/route.ts`) que es la única vía por la que el navegador llega al backend — nunca ve la URL de Railway ni el JWT del backend directamente. Todo lo anterior fue confirmado por relevamiento directo del código en esta sesión (no se asumió sobre lo reportado). Pendiente explícito, sin resolver: `apps/api/tests/` no existe todavía, y `alembic upgrade head`/boot real de `uvicorn` no se ejercitaron en vivo por falta de Docker en el sandbox actual. Ver `docs/context/decisiones.md` para el detalle cronológico de las decisiones de Railway/Vercel/JWT propio/BFF.
