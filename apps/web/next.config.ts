import type { NextConfig } from "next";

// TODO: agregar acá el proxy/rewrite hacia el backend FastAPI
// (apps/api) una vez que routers/agents.py tenga endpoints reales.
const nextConfig: NextConfig = {};

export default nextConfig;
